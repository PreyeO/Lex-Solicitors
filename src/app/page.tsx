/**
 * Home page.
 *
 * Phase 2: an intentional empty spacer so the header + footer shell can be seen
 * working (min-height makes the page scrollable to exercise the sticky navbar).
 * Landing sections from `@/components/landing/*` get composed here in Phase 3.
 */
export default function Home() {
  return <main id="top" className="min-h-[120vh] flex-1" />;
}
